idade = int(input("Digite a sua idade: "))
valor = float(input("Digite o valor da compra: "))

if valor >=500:
    desconto = 10 

elif valor >=200:
    desconto = 5

elif idade >= 60:
    desconto = desconto + 5 

else:
    print("erro")

porcentagem = valor * desconto/100
valor_final = valor - porcentagem
print (f"O valor final aplicado ao desconto é de {valor_final} reais")
